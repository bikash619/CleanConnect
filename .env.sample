# SMTP Configuration for Email Functionality
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_username
SMTP_PASSWORD=your_password
SMTP_FROM=no-reply@sparkpro.com
ADMIN_EMAIL=admin@sparkpro.com

# Replace the above values with your actual SMTP credentials
# For Gmail SMTP: 
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# Note: For Gmail, you may need to use an app password instead of your regular password

# For Office 365/Outlook:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false

# For SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false